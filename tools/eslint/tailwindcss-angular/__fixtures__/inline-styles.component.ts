/* PROTOTYPE (MAE-105) corpus fixture: a component with inline styles. */
export const component = {
    styles: [
        `
            .inline-scoped-thing {
                display: block;
            }
        `,
    ],
}
